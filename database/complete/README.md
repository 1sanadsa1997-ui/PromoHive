# 🗄️ إعداد قاعدة البيانات - نظام إبراهيم للمحاسبة

## 📋 نظرة عامة

هذا المجلد يحتوي على جميع ملفات SQL المطلوبة لإعداد قاعدة البيانات الكاملة لنظام إبراهيم للمحاسبة على Supabase.

## 🔑 معلومات الاتصال

- **Service Role Key**: `sbp_13f2ca08cd51cf75b23abb66f022e5639e66bcec`
- **Project URL**: سيتم توفيره بعد إنشاء المشروع

## 📁 ملفات الإعداد

### 1. `setup-complete.sql` - الإعداد الكامل (الملف الرئيسي)
- يحتوي على جميع الأوامر المطلوبة
- يمكن تشغيله مرة واحدة لإعداد قاعدة البيانات كاملة
- **الاستخدام**: انسخ محتوى هذا الملف وشغله في Supabase SQL Editor

### 2. `01-create-tables.sql` - إنشاء الجداول
- 14 جدول أساسي للنظام
- جميع القيود والعلاقات
- أنواع البيانات المحسّنة

### 3. `02-create-indexes.sql` - الفهارس للأداء
- 40+ فهرس استراتيجي
- فهارس مركبة للاستعلامات الشائعة
- فهارس البحث النصي

### 4. `03-row-level-security.sql` - سياسات الأمان
- 29 سياسة أمان (RLS)
- عزل بيانات المتاجر
- صلاحيات دقيقة للأدوار

### 5. `04-seed-data.sql` - البيانات الأساسية
- العملات الأساسية (TRY, SYP, USD, EUR)
- مستخدمين افتراضيين للاختبار
- بيانات تجريبية شاملة

### 6. `05-functions-triggers.sql` - الدوال والمحفزات
- دوال حساب الرواتب
- محفزات تحديث المخزون
- دوال الإحصائيات

## 🚀 خطوات الإعداد

### الطريقة السريعة (موصى بها):

1. **إنشاء مشروع Supabase جديد**
   - اذهب إلى [supabase.com](https://supabase.com)
   - أنشئ مشروع جديد
   - احفظ Project URL و API Keys

2. **تشغيل الإعداد الكامل**
   - افتح SQL Editor في Supabase Dashboard
   - انسخ محتوى `setup-complete.sql`
   - شغل الكود
   - انتظر حتى اكتمال التنفيذ

3. **التحقق من الإعداد**
   - تأكد من إنشاء جميع الجداول (14 جدول)
   - تحقق من وجود البيانات الأساسية
   - اختبر بيانات الدخول

### الطريقة التفصيلية:

إذا كنت تريد تشغيل الملفات منفصلة:

```sql
-- 1. إنشاء الجداول
\i 01-create-tables.sql

-- 2. إنشاء الفهارس
\i 02-create-indexes.sql

-- 3. إعداد الأمان
\i 03-row-level-security.sql

-- 4. إدراج البيانات
\i 04-seed-data.sql

-- 5. إنشاء الدوال
\i 05-functions-triggers.sql
```

## 🔐 بيانات الدخول الافتراضية

### 1. مدير النظام الرئيسي
- **البريد**: `admin@ibrahim-system.com`
- **كلمة المرور**: `SystemAdmin123!`
- **الدور**: مدير النظام
- **الصلاحيات**: كاملة على جميع المتاجر

### 2. مدير المتجر (إبراهيم)
- **البريد**: `ibrahim@store.com`
- **كلمة المرور**: `Ibrahim123!`
- **الدور**: مدير متجر
- **المتجر**: متجر إبراهيم التجاري
- **الاشتراك**: تجربة مجانية 30 يوم

### 3. المحاسب
- **البريد**: `accountant@store.com`
- **كلمة المرور**: `Ahmed123!`
- **الدور**: محاسب
- **التابع لـ**: متجر إبراهيم

### 4. مدير المستودع
- **البريد**: `warehouse@store.com`
- **كلمة المرور**: `Mohammed123!`
- **الدور**: مدير مستودع
- **التابع لـ**: متجر إبراهيم

## 📊 البيانات التجريبية المتضمنة

### العملاء والموردين
- شركة الأمل التجارية (عميل)
- مورد المواد الغذائية (مورد)
- متجر الورود (عميل)

### المنتجات
- أرز بسمتي (FOOD-001)
- مسحوق غسيل (CLEAN-001)
- عصير برتقال (DRINK-001)

### الفواتير
- فواتير صادرة وواردة تجريبية
- حالات مختلفة (مدفوعة، معلقة)

### الموظفين
- 3 موظفين تجريبيين
- رواتب وحركات مالية

## 🔧 إعداد متغيرات البيئة

بعد إعداد قاعدة البيانات، أضف هذه المتغيرات في Netlify:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=sbp_13f2ca08cd51cf75b23abb66f022e5639e66bcec
```

## 📈 الجداول المنشأة

| الجدول | الوصف | عدد الأعمدة |
|--------|--------|-------------|
| `users` | المستخدمين والمتاجر | 15 |
| `currencies` | العملات | 4 |
| `partners` | العملاء والموردين | 11 |
| `invoices_in` | الفواتير الواردة | 16 |
| `invoices_out` | الفواتير الصادرة | 16 |
| `inventory_items` | المنتجات | 15 |
| `inventory_movements` | حركات المخزون | 13 |
| `employees` | الموظفين | 16 |
| `employee_transactions` | معاملات الموظفين | 13 |
| `payroll` | كشوف الرواتب | 17 |
| `alerts` | التنبيهات | 12 |
| `settings` | الإعدادات | 7 |
| `audit_logs` | سجل النشاطات | 9 |

## 🛡️ الأمان

### Row Level Security (RLS)
- مفعل على جميع الجداول
- عزل بيانات المتاجر
- صلاحيات دقيقة حسب الدور

### تشفير كلمات المرور
- bcrypt مع salt rounds = 10
- كلمات مرور قوية افتراضية
- إمكانية تغيير كلمة المرور

### صلاحيات الأدوار
- **system_admin**: كامل النظام
- **store_owner**: متجره فقط
- **accountant**: المحاسبة والفواتير
- **warehouse_manager**: المخزون فقط
- **employee**: بيانات شخصية فقط

## 🔍 استكشاف الأخطاء

### مشاكل شائعة:

1. **خطأ في الصلاحيات**
   ```sql
   -- تأكد من تفعيل RLS
   SELECT schemaname, tablename, rowsecurity 
   FROM pg_tables WHERE schemaname = 'public';
   ```

2. **مشاكل في المفاتيح الخارجية**
   ```sql
   -- تحقق من القيود
   SELECT * FROM information_schema.table_constraints 
   WHERE constraint_type = 'FOREIGN KEY';
   ```

3. **بيانات مفقودة**
   ```sql
   -- تحقق من البيانات الأساسية
   SELECT 'users' as table_name, count(*) FROM users
   UNION ALL
   SELECT 'currencies', count(*) FROM currencies;
   ```

## 📞 الدعم

- **WhatsApp**: +963 994 054 027
- **البريد**: systemibrahem@gmail.com
- **النظام المباشر**: https://ibrahim-accounting-system-2024.netlify.app

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي**: احرص على عمل backup دوري
2. **التحديثات**: تابع التحديثات الأمنية
3. **المراقبة**: راقب استخدام قاعدة البيانات
4. **الأداء**: راجع الفهارس دورياً

---

**تم إعداد قاعدة البيانات بنجاح! 🎉**

